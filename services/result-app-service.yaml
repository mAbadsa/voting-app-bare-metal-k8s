apiVersion: v1
kind: Service
metadata:
  name: result-service
  namespace: voting-app
spec:
  type: NodePort
  selector:
    app: result
  ports:
    - name: result-service
      port: 8081
      targetPort: 80
      nodePort: 31001